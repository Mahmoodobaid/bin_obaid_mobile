import 'package:flutter/material.dart';
class InvoiceListScreen extends StatelessWidget {
  const InvoiceListScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('قائمة الفواتير')), body: const Center(child: Text('قيد التطوير')));
}
