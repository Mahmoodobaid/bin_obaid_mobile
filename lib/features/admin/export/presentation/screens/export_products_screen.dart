import 'package:flutter/material.dart';
class ExportProductsScreen extends StatelessWidget {
  const ExportProductsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('تصدير المنتجات')),
    body: const Center(child: Text('قيد التطوير')),
  );
}
